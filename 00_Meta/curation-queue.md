---
created: 2026-05-16
updated: 2026-05-16
type: meta
generated_by: hirono propose-curation
status: pending-review
---

# Curation Queue — 2026-05-16

Generated by `hirono propose-curation`. Review each proposal: tick `[x]` to approve, leave `[ ]` to skip. Items NOT ticked are ignored by `hirono apply-queue`.

**To execute approved items**:

```
npx tsx tools/bin/hirono.ts apply-queue
```

Or auto-apply only high-confidence items:

```
npx tsx tools/bin/hirono.ts apply-queue --auto-apply high
```

## Proposals (18 actionable, 26 skipped)

### 1. Merge `Triton` → `OpenAI Triton`  [confidence: high]

- [ ] approved

**Rationale**: Triton (_seen, refs=2) is a lowercase alias of OpenAI Triton (active, refs=9); definitions are identical; merge target already has the canonical name

```
hirono merge-entities Triton --into "OpenAI Triton" --reason "Same product — both describe OpenAI's Python DSL + compiler for GPU kernels; 'Triton' _seen stub is an alias with no independent observations"
```

### 2. Merge `Speculative Sampling` → `Speculative Decoding`  [confidence: high]

- [ ] approved

**Rationale**: Speculative Sampling (_seen, refs=0) describes the same draft-then-verify inference technique as Speculative Decoding (active, refs=11); 'sampling' is one framing of the same algorithm

```
hirono merge-entities "Speculative Sampling" --into "Speculative Decoding" --reason "Speculative Sampling is the algorithmic mechanism inside Speculative Decoding; no independent identity — refs=0 stub with no observations"
```

### 3. Merge `Attention DP` → `DP Attention`  [confidence: medium]

- [ ] approved

**Rationale**: Attention DP (_seen) describes a TRT-LLM configuration for data-parallel attention; DP Attention (active) is the canonical entity for this pattern; zero observations in Attention DP make merge safe

```
hirono merge-entities "Attention DP" --into "DP Attention" --reason "Both refer to data-parallel attention distribution; 'Attention DP' (_seen, refs=0) is an alias coined in one context for what DP Attention (active, refs=7) generalizes"
```

### 4. Merge `FSDP2` → `FSDP`  [confidence: medium]

- [ ] approved

**Rationale**: FSDP2 (_seen, refs=1) is PyTorch's rewrite of FSDP using DTensor; the Observations in FSDP already reference FSDP2 by name, making it a sub-feature not a separate entity

```
hirono merge-entities FSDP2 --into FSDP --reason "FSDP2 is the v2 successor of FSDP1; same project, same entity — refs=1 with one DTensor observation that belongs under FSDP umbrella"
```

### 5. Merge `FlashAttention-3` → `FlashAttention`  [confidence: medium]

- [ ] approved

**Rationale**: FlashAttention (active) already describes FA-3's ping-pong scheduling in its Synthesis; FlashAttention-3 (_seen, refs=2) has valuable FP8 layout observations that should live under the umbrella entity

```
hirono merge-entities FlashAttention-3 --into FlashAttention --reason "FA-3 is v3 of the same FlashAttention project; FlashAttention (active, refs=18) already covers FA-3 in its Synthesis; keeping a separate entity creates wikilink fragmentation"
```

### 6. Delete orphan `ICLR`  [confidence: medium]

- [ ] approved

**Rationale**: ICLR was scaffolded as a venue reference, not as a technical concept; the corpus tracks algorithms and systems, not conferences; its only role was as TurboQuant's publication venue

```
hirono bulk-delete-orphans --confirm ICLR
```

### 7. Delete orphan `Hermes`  [confidence: medium]

- [ ] approved

**Rationale**: Hermes appears once as a passing reference in a Garry Tan setup description; with refs=0 and no observations, it has no semantic value in the wiki graph

```
hirono bulk-delete-orphans --confirm Hermes
```

### 8. Delete orphan `vibe-replay`  [confidence: medium]

- [ ] approved

**Rationale**: vibe-replay is a minor developer tool mentioned once; refs=0 and empty observations indicate no cross-source relevance

```
hirono bulk-delete-orphans --confirm vibe-replay
```

### 9. Delete orphan `SGLang mini-LB`  [confidence: medium]

- [ ] approved

**Rationale**: Deprecated component with refs=0 and no observations; definition acknowledges obsolescence

```
hirono bulk-delete-orphans --confirm "SGLang mini-LB"
```

### 10. Delete orphan `SGLang model gateway`  [confidence: medium]

- [ ] approved

**Rationale**: Sub-component entity with refs=0; future observations would more naturally live under SGLang

```
hirono bulk-delete-orphans --confirm "SGLang model gateway"
```

### 11. Delete orphan `turboquant_plus`  [confidence: low]

- [ ] approved

**Rationale**: turboquant_plus is Tom Turney's reproduction of TurboQuant; if TurboQuant matures, turboquant_plus information belongs there as an observation

```
hirono bulk-delete-orphans --confirm turboquant_plus
```

### 12. Refine Entity `DeepSeek-V3` Synthesis  [confidence: high]

- [ ] approved

**Rationale**: refs=34 makes this the most-cited entity without a real Synthesis; Observations are dense and span multiple sources

```
hirono refine-entity DeepSeek-V3    # → prompt; operator spawns Sonnet → apply
```

### 13. Refine Entity `Qwen` Synthesis  [confidence: high]

- [ ] approved

**Rationale**: Second-highest-cited entity without a real Synthesis; Observations are already populated with technical specifics

```
hirono refine-entity Qwen    # → prompt; operator spawns Sonnet → apply
```

### 14. Refine Entity `GB200` Synthesis  [confidence: high]

- [ ] approved

**Rationale**: Active entity with refs=13, placeholder Synthesis, and dense Observations — high ROI refine target

```
hirono refine-entity GB200    # → prompt; operator spawns Sonnet → apply
```

### 15. Refine Entity `NVIDIA Cosmos` Synthesis  [confidence: medium]

- [ ] approved

**Rationale**: Active entity with refs=8, placeholder Synthesis; Observations span world-model taxonomy and video generation benchmarks

```
hirono refine-entity "NVIDIA Cosmos"    # → prompt; operator spawns Sonnet → apply
```

### 16. Refine Entity `FSDP` Synthesis  [confidence: medium]

- [ ] approved

**Rationale**: Active training-infrastructure entity with placeholder Synthesis and substantive Observations

```
hirono refine-entity FSDP    # → prompt; operator spawns Sonnet → apply
```

### 17. Add `## Comparison` heading to Topic `Inference-Optimized Accelerators`  [confidence: medium]

- [ ] approved

**Rationale**: Existing prose already explicitly contrasts Ironwood specs vs A100 pods on multiple shared axes with sourced numbers; a ## Comparison section would formalize what the prose already argues

```
hirono add-comparison-heading "Inference-Optimized Accelerators" --reason "Topic's central job IS contrasting named accelerators (TPU Ironwood vs NVIDIA GPU vs Groq LPU vs Cerebras) on shared axes: perf/W, HBM capacity, interconnect, programming model — a head-to-head table is load-bearing here"
```

### 18. Add `## Comparison` heading to Topic `Numerical Precision`  [confidence: medium]

- [ ] approved

**Rationale**: Numerical precision as a topic is fundamentally about tradeoffs between precision formats; a comparison table on dynamic range / exponent bits / hardware support / typical use case would be the canonical content

```
hirono add-comparison-heading "Numerical Precision" --reason "Topic contrasts FP8 (E4M3/E5M2), BF16, FP4, MXFP8, TF32 on shared axes (dynamic range, hardware support, use case) — contrasts are the central content of this topic"
```

## Skipped (Sonnet judged these to be false positives)

- DeepSeek-R1 ↔ DeepSeek-V3 (similarity 82%) — Similarity score driven by shared corpus context, not semantic equivalence; orthogonal content
- DeepSeek-V3.2 ↔ DeepSeek-V3 (similarity 85%) — V3.2 has DSA-specific observations not present in V3; merging would lose an architectural distinction this corpus specifically tracks
- B200 ↔ GB200 (similarity 80%) — Canonical NVIDIA SKU distinction: B200=chip, GB200=superchip; both have independent observations and refs; merging would conflate hardware specs
- GB200 ↔ GB300 (similarity 80%) — Different CUDA compute capabilities and HBM configs; corpus tracks these as separate hardware targets in inference benchmarks
- B300 ↔ GB300 (similarity 80%) — Both are genuinely different SKUs; same pattern as B200/GB200 distinction; refs=1 each but distinct system-level roles
- Llama ↔ Ollama (similarity 83%) — 83% similarity is a name-overlap artifact ('llama' substring in 'ollama'); semantically unrelated
- NVIDIA Cosmos ↔ NVIDIA OSMO (similarity 85%) — Similarity driven by shared 'NVIDIA physical AI' context; Cosmos=models, OSMO=orchestration; keeping them separate reflects real product distinction
- Qwen ↔ Qwen3 (similarity 80%) — Qwen3 scaffolded recently (2026-05-16); it is a distinct generation worth tracking separately from the broader Qwen family entity
- DeepSeek-R1 ↔ DeepSeek-V2 (similarity 82%) — V2 introduced MLA which V3/R1 builds on; it's a precursor architectural milestone, not a duplicate
- DeepSeek-V3.2 ↔ DeepSeek-V2 (similarity 85%) — Similarity is corpus-context-driven; architecturally orthogonal
- FSDP ↔ FSDP2 duplicate pair — Already proposed as merge; skip the duplicate-pair finding
- 01_Topics/LLM Pretraining.md: comparison-opportunity with 26 active-tier entities — Survey of heterogeneous sub-concerns; contrast markers are incidental co-mentions, not load-bearing comparisons on shared axes
- 01_Topics/RL Post-Training.md: comparison-opportunity with 23 active-tier entities — Incidental entity mentions dominate; genuine comparisons (PPO vs GRPO tradeoffs) are better handled as prose or a focused sub-comparison
- 01_Topics/Training Infrastructure.md: comparison-opportunity with 19 active-tier entities — Infrastructure comparison tables go stale quickly; topic's value is synthesis of common patterns, not side-by-side feature comparison
- 01_Topics/Minimal-Implementation Pedagogy.md: comparison-opportunity with 3 active-tier entities — 3 entities is below threshold for a meaningful comparison table; the prose contrasts minimal vs full implementations as a pedagogical point already well-served by prose
- 01_Topics/AI Data Centers.md: comparison-opportunity with 17 active-tier entities — AI Data Centers is a broad infrastructure topic; entities like NVLink, InfiniBand, Stargate, CoreWeave don't share enough axes for a single load-bearing comparison
- 01_Topics/LLM Architectures.md: comparison-opportunity with 19 active-tier entities — Topic already has rich comparative prose (MLA vs CSA, MoE variants, attention types); a formal heading would redundantly section what's already well-organized
- 01_Topics/GPU Profiling.md: comparison-opportunity with 15 active-tier entities — There may be a genuine profiler-tool comparison sub-section worth adding, but the full 15-entity scope is too broad; defer pending targeted sourcing
- 01_Topics/MoE Training.md: comparison-opportunity with 8 active-tier entities — Defensible comparison opportunity but premature without a dedicated refine
- 01_Topics/Tensor Core Programming.md: comparison-opportunity with 11 active-tier entities — Borderline case; existing prose is structured enough that a formal heading would fragment the narrative
- 01_Topics/Data Loading Pipelines.md: comparison-opportunity with 7 active-tier entities — Premature to add comparison heading before more Sources are ingested on this topic
- 01_Topics/Multi-Tenancy on GPUs.md: comparison-opportunity with 6 active-tier entities — Genuine comparison axes exist but corpus evidence for this topic is sparse
- 01_Topics/GPU Utilization.md: comparison-opportunity with 9 active-tier entities — 9 entities with diverse measurement-domain coverage; adding a comparison heading prematurely would create a sparse table
- 01_Topics/GPU Performance Modeling.md: comparison-opportunity with 6 active-tier entities — Performance modeling is an analytical framework survey, not a framework comparison; formal comparison heading not warranted
- 01_Topics/Quantization.md: comparison-opportunity with 5 active-tier entities — Comparison is already embedded in the prose structure; formal heading would fragment rather than clarify
- 01_Topics/GPU Kernel Scheduling.md: comparison-opportunity with 7 active-tier entities — Instructional explainer of hardware mechanisms, not competing design choices; no sourced benchmark numbers to populate a comparison table
