{
  "name": "@hirono/wiki-tools",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Tools for the personal wiki: preprocess, sync, ingest, reconcile. Adapter layer on top of lark-hirono.",
  "scripts": {
    "preprocess": "tsx bin/preprocess.ts",
    "test": "node --import tsx --test __tests__/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@joplin/turndown-plugin-gfm": "^1.0.64",
    "@types/jsdom": "^28.0.1",
    "@types/turndown": "^5.0.6",
    "gray-matter": "^4.0.3",
    "jsdom": "^29.0.2",
    "turndown": "^7.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
